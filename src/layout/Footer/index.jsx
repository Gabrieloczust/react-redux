import React from 'react';

import './styles.css';

export default () => (
    <footer className="l-footer navbar">
        <div className="container">
            <span className="text-muted">Shopping-Car &copy; {new Date().getFullYear()}</span>
        </div>
    </footer>
);
